server.port: ${PORT:8080}

spring:
  main:
    banner-mode: "off"

keybout:
  latency: 0

logging.level:
  ROOT: INFO

---

spring.config.activate.on-profile: color

spring.output.ansi.enabled: ALWAYS

---

spring.config.activate.on-profile: dev

logging.level:
  com.adeliosys: TRACE
